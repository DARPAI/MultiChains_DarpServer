# API key from etherscan.io registration
ETHERSCAN_API_KEY=etherscan.io register to get api key

# Get cookie from gmgn.ai by accessing with browser and checking F12 dev tools
GMGN_COOKIE=get cookie from gmgn.ai browser F12

USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:133.0) Gecko/20100101 Firefox/131.0"

# Get X-Auth header from ave.ai by accessing with browser and checking F12 dev tools
AVE_AUTH=get X-Auth header from ave.ai browser F12

# Proxy configuration - fill in your own proxy details
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password
PROXY_HOST=your_proxy_host
PROXY_PORT=your_proxy_port